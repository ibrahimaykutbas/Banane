import {StyleSheet} from 'react-native';

import colors from '../../../styles/color';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    color: colors.purple,
    margin: 5,
    fontSize: 150,
  },
});
